services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      TZ: Europe/Amsterdam
    command: ["postgres", "-c", "timezone=Europe/Amsterdam", "-c", "log_timezone=Europe/Amsterdam"]
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 3s
      timeout: 5s
      retries: 10

    #ports:
    #  - "5432:5432"
  app:
    build:
      context: .
      args:
        APP_VERSION: ${APP_VERSION:-dev}
    # Server runs as APP_UID (see docker-entrypoint.sh); entrypoint briefly runs as root to chmod the receipts bind mount.
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      MIGRATIONS_DIR: /app/migrations
      LISTEN_ADDR: ":8080"
      TZ: Europe/Amsterdam
      APP_UID: ${DOCKER_UID:-1000}
      APP_GID: ${DOCKER_GID:-1000}
    volumes:
      - /home/sjoerd/lunchkraam/data/receipts:/app/data/receipts
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata: